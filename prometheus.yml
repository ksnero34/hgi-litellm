global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'litellm'
    authorization:
      type: Bearer
      credentials_file: /run/secrets/litellm_metrics_token
    static_configs:
      - targets: ['litellm:4000']  # Assuming Litellm exposes metrics at port 4000
